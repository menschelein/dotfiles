# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-O2 -pipe -march=skylake"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
CPU_FLAGS_X86="avx avx2 f16c fma3 mmx mmxext pclmul popcnt sse sse2 sse3 sse4_1 sse4_2 ssse3"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

MAKEOPTS="-j3"

#My settings
LINGUAS="de_AT"
L10N="de-AT"

#Disable
USE="${USE} -a52 -adds -aqua -bindist -blueray -bluetooth -cdda -cdr -dvd -dvdr -epub -gnome -gpm -kde -mtp -pdf -perl -postscript -pulseaudio -sqlite -systemd"
#Enable
USE="${USE} aalib abi_x86_32 alsa crypt gnome-keyring haptic imlib jpeg lcms libdrm lto minizip mng mp4 opus png postproc python_targets_python3_8 ruby_targets_ruby27 savedconfig secure-delete sqlite svg threads tiff tk unicode vaapi vdpau vulkan wayland webp wifi wmf x264 x265 xpm"

ACCEPT_LICENSE="@FREE linux-fw-redistributable no-source-code all-rights-reserved unRAR ValveSteamLicense"

VIDEO_CARDS="intel"